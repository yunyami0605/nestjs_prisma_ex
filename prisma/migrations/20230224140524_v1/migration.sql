-- CreateTable
CREATE TABLE "User" (
    "id" SERIAL NOT NULL,

    CONSTRAINT "User_pkey" PRIMARY KEY ("id")
);

-- CreateTable
CREATE TABLE "info" (
    "id" SERIAL NOT NULL,

    CONSTRAINT "info_pkey" PRIMARY KEY ("id")
);
